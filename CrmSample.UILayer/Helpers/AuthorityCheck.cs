using CrmSample.BusinessLayer.Abstract;
using CrmSample.DataAccessLayer.Concrete;
using CrmSample.EntityLayer.Concrete;
using DocumentFormat.OpenXml.Spreadsheet;
using Microsoft.AspNetCore.Identity;
using System.Threading.Tasks;

namespace CrmSample.UILayer.Helpers
{
    public  class AuthorityCheck
    {

        //private readonly UserManager<AppUser> _userManager;

        //public AuthorityCheck(UserManager<AppUser> userManager)
        //{
        //    _userManager = userManager;
        //}

        //public async Task UserAuthorityCheckAsync()
        //{
        //    var values = await _userManager.FindByNameAsync(User.Identity.Name);
        //    var isUserAdmin = userRoles.Contains("admin");

        //    ViewBag.isUserAdmin = isUserAdmin;
        //}
    }
}
